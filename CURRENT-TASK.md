# Daily Autodigital Shelf Current Task

## Active Goal

Keep the daily generator, public site, scheduled task, GitHub push flow, and monetization boundary stable.

## Current Completion Edge

- Stable: generator, 21-pack starter archive, 21 individual product ZIPs, 21 support-aware generated download landing pages, public product-feed files (`product-feed.json`, `product-feed.xml`, `product-feed.csv`), generated starter bundle ZIP, marketplace import kit, 5 generated topic pages plus topic JSON, 5 generated support-backed offer pages plus offer JSON, 4 generated store-readiness policy pages, generated support page, generated `pay-what-you-can.html` support-first bundle funnel, `llms.txt`/`llms-full.txt`, current June 2, 2026 pack, static site files, seller-copy output, JSON/RSS/Atom feeds, robots/sitemap metadata, social card metadata, richer JSON-LD, catalog/import JSON and CSV support monetization metadata plus `download_page_url`, branded CalmSprout product/support/support-intent metadata, current pack `DonateAction` JSON-LD in support mode, public support destination, CalmSprout branded bridge routes (`/daily-shelf`, `/daily-shelf/today`, `/daily-shelf/products`, `/daily-shelf/products/<pack-slug>`, `/daily-shelf/products/<pack-slug>/support`, `/daily-shelf/products/<pack-slug>/support/go`, `/daily-shelf/product-sitemap.xml`, `/daily-shelf/browse`, `/daily-shelf/packs/<pack-slug>/...`, `/daily-shelf/today.zip`, `/daily-shelf/current.zip`, `/daily-shelf/downloads/<pack>.zip`, `/daily-shelf/downloads/<pack>.html`, `/daily-shelf/product-feed.json`, `/daily-shelf/product-feed.xml`, `/daily-shelf/product-feed.csv`, `/daily-shelf/styles.css`, `/daily-shelf/pay`, `/daily-shelf/bundle`, `/daily-shelf/status`, `/daily-shelf/catalog.json`, `/daily-shelf/store-listings.json`, `/daily-shelf/feed.json`, `/daily-shelf/starter.zip`, LLM files, sitemap, and IndexNow key), Daily Shelf IndexNow submission, CalmSprout branded IndexNow submission, ledger/status output, run wrapper, verifier, daily scheduled task, watchdog scheduled task, GitHub Actions fallback publisher, GitHub Pages, HUMANi lane registration.
- Remaining gap: a support path is connected, but actual daily revenue is not proven and product checkout/store payment is not connected. Do not fake this in the site.
- Latest verification: `verify-system.ps1` requires at least 21 pack manifests, 21 generated pack download ZIPs with `SUPPORT.txt`, 21 support-aware generated download pages, public product-feed JSON/XML/CSV files, the marketplace import kit with product-feed files, starter archive `SUPPORT.txt`, topic pages/topic JSON, offer pages/offer JSON, 4 policy pages, support page, pay-what-you-can page, `llms.txt`, `llms-full.txt`, JSON/RSS/Atom feed paths, social metadata on generated public pages, static product `Offer`/`FAQPage` schema on the current pack page, `download_page_url` in catalog/import rows, the public support destination when support mode is connected, the GitHub fallback workflow, the daily task, and the watchdog task. It passed on 2026-06-02 with `files_checked: 43`, `bundle_bytes: 375773`, current `download_bytes: 6875`, `store_import_zip_bytes: 197650`, `monetization_enabled: true`, `support_connected: true`, and `store_connected: false`. Latest Pages run `26816697269` succeeded for commit `0a5d3da`.
- Latest bridge verification: CalmSprout Worker commit `9401218` (`Add Daily Shelf bridge aliases`) deployed as Cloudflare version `c3a3f5b4-551b-4074-95e9-a0264ff88f1c`; live `/daily-shelf/today` and `/daily-shelf/bundle` returned HTTP 200 first-party pages with support links and checkout-boundary text, `/daily-shelf/status` resolved to public `status.json` with `store_connected: false`, browser QA found no console issues, and IndexNow accepted 13 CalmSprout bridge/discovery URLs with HTTP 200.
- Latest metadata verification: commit `e50f94e` (`Add support monetization metadata`) deployed through GitHub Pages run `26808927786`; live `catalog.json`, `imports/store-listings.json`, and `status.json` include support/pay URLs and monetization destination fields, the current pack page includes `DonateAction`, `verify-system.ps1` passed with `bundle_bytes: 328177`, `store_import_zip_bytes: 162402`, `support_connected: true`, and `store_connected: false`, and IndexNow accepted 8 changed shelf URLs with HTTP 200.
- Latest CalmSprout data verification: Worker commit `0c509a0` (`Add Daily Shelf data aliases`) deployed as Cloudflare version `737bd883-dc0a-4610-ac60-4e29ec4b29d1`; live branded status/catalog/import/feed routes returned HTTP 200, metadata routes include support/pay fields, `/daily-shelf/starter.zip` returned 328,177 bytes, CalmSprout `llms.txt` and `sitemap.xml` include the data aliases, and IndexNow accepted 22 CalmSprout bridge/data URLs with HTTP 200.
- Latest CalmSprout IndexNow automation: Daily Shelf commit `4bad33c` (`Add CalmSprout IndexNow automation`) pushed to `main`; Pages run `26809864174` succeeded. `tools/submit_calmsprout_indexnow.py` is wired into `run-daily.ps1` and the GitHub fallback publisher, direct `--all` submission accepted 25 CalmSprout URLs with HTTP 200 after key verification, follow-up dry runs returned `submit_count: 0`, and `verify-system.ps1` now passes with `files_checked: 40`.
- Latest CalmSprout current-pack verification: Worker commit `6786fe0` deployed as version `91486cba-f17b-4525-8ff8-6f99baab0399`; live `/daily-shelf` and `/daily-shelf/today` dynamically show `One-Page SOP Builder`, support state, and checkout boundary; live `/daily-shelf/today.zip` and `/daily-shelf/current.zip` return 5,869-byte ZIP responses. Daily Shelf commit `d3221af` makes these routes part of the unattended CalmSprout IndexNow submitter; forced submission accepted 27 URLs and follow-up dry runs returned `submit_count: 0`.
- Latest CalmSprout product-catalog verification: Worker commit `557e6c5` deployed as version `51bd5adb-e134-4e90-8be4-e628d5727368`; live `/daily-shelf/products` and `/daily-shelf/browse` dynamically show 21 catalog product cards, support state, and checkout boundary; live `/daily-shelf/downloads/2026-06-02-one-page-sop.zip` returns the 5,869-byte current pack ZIP. Daily Shelf commit `c68c63a` makes these routes part of the unattended CalmSprout IndexNow submitter; IndexNow accepted the product and browse URLs and follow-up dry runs returned `submit_count: 0`.
- Latest CalmSprout product-detail verification: Worker commit `9194d08` deployed as version `2dc1f4d3-a6bf-4af4-aec7-9fef87539100`; live `/daily-shelf/products/2026-06-02-one-page-sop` dynamically shows the current pack product detail page with related packs, support state, checkout boundary, and first-party ZIP CTA; live `/daily-shelf/packs/2026-06-02-one-page-sop/` proxies the public pack page. Daily Shelf commit `cc21a51` makes all 21 catalog-derived detail routes part of the unattended CalmSprout IndexNow submitter; IndexNow accepted the 21 product detail URLs and follow-up dry runs returned `submit_count: 0`.
- Latest CalmSprout product-sitemap verification: Worker commit `d8af561` deployed as version `738a15ee-3f18-44b1-b92d-cf75d865f0f6`; live `/sitemap.xml` now includes the 21 catalog-derived product detail URLs and product sitemap URL; live `/daily-shelf/product-sitemap.xml` lists 21 product detail URLs. Daily Shelf commit `8b42dce` makes the sitemap routes part of the unattended CalmSprout IndexNow submitter; IndexNow accepted both sitemap URLs and follow-up dry runs returned `submit_count: 0`.
- Latest CalmSprout product-support verification: Worker commit `3001f49` deployed as version `8777e666-af2d-40db-bcbc-317dccf50560`; live `/daily-shelf/products/2026-06-02-one-page-sop/support` dynamically shows a product-specific support funnel with Square support CTA, suggested support tiers, and checkout boundary. Daily Shelf commit `41949c7` makes all 21 catalog-derived support routes part of the unattended CalmSprout IndexNow submitter; IndexNow accepted the 21 product support URLs and follow-up dry runs returned `submit_count: 0`.
- Latest CalmSprout support-intent verification: Worker commit `6b5e7bf` deployed as version `f571cf59-c42e-4781-ad71-7fc8e2861646`; live `/daily-shelf/products/2026-06-02-one-page-sop/support` returns HTTP 200 and its primary CTA points to `/daily-shelf/products/2026-06-02-one-page-sop/support/go`. Live `/support/go` returns HTTP 302 to the Square support page with `utm_source=calmsprout`, `utm_medium=daily_shelf`, `utm_campaign=product_support`, and `utm_content=2026-06-02-one-page-sop`; browser QA found no console messages and no desktop/mobile horizontal overflow. CalmSprout record commit `a255b23` records the deploy locally. Daily Shelf `tools/submit_calmsprout_indexnow.py --all --dry-run` returned `candidate_count: 72` and `submit_count: 0`.
- Latest branded support-intent metadata: Daily Shelf commit `6179a74` adds `branded_product_url`, `branded_support_url`, and `branded_support_intent_url` to catalog/import rows, adds current branded support URLs to `status.json`, and adds branded support-intent lines to shelf `llms.txt`/`llms-full.txt`. Pages run `26813575995` succeeded; live status/catalog files on GitHub Pages and CalmSprout expose `/support/go`; IndexNow accepted 9 GitHub Pages URLs and 65 CalmSprout URLs with HTTP 200, and follow-up dry runs returned `submit_count: 0`.
- Latest CalmSprout LLM support-intent deploy: Worker commit `2821d51` deployed as version `37cbc214-76e9-4568-b3ed-8656c70db132`; live `/llms.txt` and `/llms-full.txt` include `/support/go` and `branded_support_intent_url`, and cache-busted `/daily-shelf/llms.txt` plus `/daily-shelf/llms-full.txt` verified the same deployed content. CalmSprout record commit `392479e` records the deploy locally.
- Latest CalmSprout product offer/FAQ schema deploy: Worker commit `187a1b6` deployed as version `4108d3fb-3cb6-444f-88db-9914c995a967`; live `/daily-shelf/products/2026-06-02-one-page-sop` returns HTTP 200 with JSON-LD `CreativeWork`/`Product`, an honest free-public-download `Offer`, product-specific `DonateAction` to `/support/go`, `FAQPage`, and a visible FAQ section. Desktop/mobile QA found no console messages and no horizontal overflow. CalmSprout record commit `874504d` records the deploy locally. Forced CalmSprout IndexNow submission accepted 72 URLs with HTTP 200, and the follow-up dry run returned `submit_count: 0`.
- Latest static pack product offer/FAQ schema: Daily Shelf commit `90a4853` refreshes deterministic historical pack pages and gives all 21 static pack pages JSON-LD `CreativeWork`/`Product`, free-public-download `Offer`, product-specific `DonateAction` to CalmSprout `/support/go`, `FAQPage`, and visible FAQ copy. Pages run `26814777758` succeeded. Live HTTPS browser QA on the current pack page found `CreativeWork`, `Product`, `FAQPage`, `Offer` price `0.00` `USD`, the product support-intent href, visible FAQ, no console warnings/errors, and no horizontal overflow.
- Latest CalmSprout proxied pack indexing: Daily Shelf commit `bd1b187` adds `/daily-shelf/packs/{slug}/` and `/daily-shelf/downloads/{slug}.zip` to the CalmSprout IndexNow candidate set. Pages run `26814868699` succeeded. IndexNow accepted 24 GitHub Pages static schema URLs, 9 CalmSprout data/bundle URLs, and then 42 newly tracked CalmSprout proxied pack/ZIP URLs with HTTP 200; follow-up dry runs returned `submit_count: 0` for both hosts.
- Latest ZIP support cards: Daily Shelf commit `6cbeba1` adds `SUPPORT.txt` to every generated pack ZIP and to the starter archive, with pack page, ZIP URL, product support page, product-specific `/support/go`, voluntary-support text, and the `Product checkout is not connected` boundary. Pages run `26815277958` succeeded; live ZIP checks verified the current product ZIP and starter archive both contain `SUPPORT.txt`, `/support/go`, and the checkout boundary.
- Latest direct ZIP artifact indexing: Daily Shelf commit `ab350a5` adds starter archive ZIP, store upload kit ZIP, current ZIP, and all catalog product ZIPs to the GitHub Pages IndexNow submitter. Pages run `26815383934` succeeded. IndexNow accepted 23 direct GitHub Pages ZIP artifact URLs and 30 CalmSprout download/data URLs with HTTP 200 after the support-card change.
- Latest support-aware download pages: Daily Shelf commit `de0b29a` generates 21 public `docs/downloads/<pack>.html` landing pages with `DownloadAction`, product-specific `DonateAction`, direct ZIP buttons, voluntary support text, and the product-checkout boundary. Catalog/import rows now expose `download_page_url`; `status.json` exposes `today_download_page` and `pack_download_page_count`; sitemap and IndexNow include the generated pages. GitHub Pages run `26816113707` succeeded. Live browser QA passed on the GitHub Pages download page, CalmSprout product page, and CalmSprout proxied download page with no console warnings/errors and no horizontal overflow. CalmSprout commits `53b3183` and `1d44688` route product CTAs through the download pages and proxy `/daily-shelf/styles.css`; latest Worker deploy version is `4be3afcc-a783-43d4-a442-32777a74acfd`. IndexNow accepted 88 GitHub Pages URLs plus 100 and then 31 CalmSprout URLs with HTTP 200; follow-up dry runs returned `submit_count: 0` for both hosts. Product checkout remains disconnected and daily revenue is not proven.
- Latest public product feed: Daily Shelf commit `0a5d3da` adds `product-feed.json`, `product-feed.xml`, and `product-feed.csv` with 21 public product/download/support records, product-specific `/support/go` fields, and an explicit checkout boundary. The feed files are linked from the homepage, sitemap, status, LLM discovery files, and store upload kit. Pages run `26816697269` succeeded; live GitHub Pages and CalmSprout feed URLs returned HTTP 200 with 21 items, `One-Page SOP Builder`, `/support/go`, and `Product checkout is not connected`. CalmSprout commit `8d70dc3` proxies the three feed routes and deployed as Worker version `da74cb60-539e-4717-8be2-696257d252b3`. IndexNow accepted 11 GitHub Pages URLs and 15 CalmSprout URLs with HTTP 200; follow-up dry runs returned `submit_count: 0` for both hosts. Product checkout remains disconnected and daily revenue is not proven.

## Recommended Next Moves

1. Keep pack quality useful and original before adding more templates or topic rules.
2. Upgrade the connected support path to real product checkout only after a legitimate store/payout account exists.
3. Keep protocol, handoff, and queue state honest as work changes.
